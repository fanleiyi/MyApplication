<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
     xmlns:tools="http://schemas.android.com/tools" android:id="@+id/activity_main"

    android:layout_width="match_parent" android:layout_height="match_parent"

    tools:context="com.example.day03.MainActivity">
    <!--主页面内容-->
   <FrameLayout
       android:id="@+id/mainContentLayoutId"
       android:layout_width="match_parent"
       android:layout_height="wrap_content">

       <TextView
           android:id="@+id/textView"
           android:textSize="30sp"
           android:text="DrawerLayout Example"
           android:layout_width="wrap_content"
           android:layout_height="wrap_content" />
   </FrameLayout>
        <!--可隐藏的侧滑子元素-->
    <LinearLayout
        android:id="@+id/leftLayoutId"
        android:orientation="vertical"
        android:layout_width="260dp"
        android:background="#fffffccc"
        android:layout_gravity="start"
        android:layout_height="match_parent">

        <TextView
            android:id="@+id/startTextId"
            android:text="嘿嘿！"
            android:layout_width="match_parent"
            android:layout_height="48dp" />
        <ListView
            android:id="@+id/startListMenuId"
            android:layout_width="match_parent"
            android:layout_height="match_parent" >
            </ListView>
    </LinearLayout>
</android.support.v4.widget.DrawerLayout>